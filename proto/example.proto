syntax = "proto3";

package example;

message Transactions {
  repeated Transaction transactions = 1;
}

message Transaction {
    string signature = 1;
    uint64 slot = 2;
    string timestamp = 3;
    uint64 ordinal = 4;
}

